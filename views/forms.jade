extends layout

block content
  //- h1= title
  //- p Welcome to #{title}
  #container
    nav
      .nav-wrapper
        .col.s12
          a(href="/" ).breadcrumb bookmark
          a(href="/forms").breadcrumb input
    .form
     .row
        .input-field.col.s12
          input(type="text" name="title").title
          label(for="title") Title
        .input-field.col.s12
          input(type="url" name="url").url
          label(for="url") URL
        .input-field.col.s12
          select
            option(value="" disabled selected) Choose layer
            option(value="1") /
            option(value="2") 2
            //- option(value="3") 3
            //- option(value="4") 4
            label Materialize Select
        .input-field.col.s12
          button(type="submit" name="action").btn.waves-effect.waves-light Submit
            i.material-icons.right send
  </div>
